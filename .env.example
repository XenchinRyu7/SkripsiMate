# ============================================
# DEPLOYMENT CONFIGURATION
# ============================================

# Deployment Mode
# - self-hosted: Unlimited projects, nodes, members (for localhost/private hosting)
# - cloud: Enforce subscription limits (for production SaaS)
# Leave empty for auto-detect (localhost = self-hosted, vercel domains = cloud)
NEXT_PUBLIC_DEPLOYMENT_MODE=self-hosted

# Authentication Mode (Only for self-hosted)
# - multi-user: Use Firebase Auth (for teams/organizations - RECOMMENDED)
# - single-user: No authentication, direct access (for personal use only)
# Note: Cloud mode ALWAYS uses multi-user authentication
NEXT_PUBLIC_AUTH_MODE=multi-user

# ============================================
# WHEN TO USE SINGLE-USER MODE:
# ============================================
# ✅ Personal laptop/desktop deployment
# ✅ Single developer environment
# ✅ No team collaboration needed
# ❌ NOT for: Companies, campuses, teams
# ❌ NOT for: Cloud deployment
# ============================================

# Firebase Client (Public - safe for client-side)
# IMPORTANT: Must use NEXT_PUBLIC_ prefix for client-side access!
NEXT_PUBLIC_FIREBASE_API_KEY=your_firebase_api_key_here
NEXT_PUBLIC_FIREBASE_PROJECT_ID=your_project_id
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=your_project.firebaseapp.com
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=your_project.appspot.com
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=your_sender_id
NEXT_PUBLIC_FIREBASE_APP_ID=your_app_id

# Firebase Admin (Server-side only)
FIREBASE_ADMIN_PRIVATE_KEY_ID=your_private_key_id
FIREBASE_ADMIN_PRIVATE=your_private_key_here
FIREBASE_CLIENT_EMAIL=your_service_account@project.iam.gserviceaccount.com
FIREBASE_CLIENT_ID=your_client_id
FIREBASE_CLIENT_CERT=your_cert_url

# Supabase (Public anon key is safe for client-side, service role is server-only)
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_anon_key_here
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key_here

# Gemini AI (Server-side only - DO NOT expose to client)
GEMINI_API_KEY=your_gemini_api_key_here
GEMINI_MODEL=gemini-2.0-flash-exp
# Available models:
# - gemini-1.5-flash (Fast, cost-effective, recommended)
# - gemini-1.5-pro (More capable, slower, more expensive)
# - gemini-2.0-flash-thinking-exp-1219 (Latest experimental with reasoning)
GEMINI_EMBEDDING_MODEL=text-embedding-004

